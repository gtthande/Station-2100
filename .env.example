# Supabase Configuration
# Get these values from your Supabase project dashboard
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-key-here

# Supabase Service Role Key (NEVER expose this to the client!)
# Used only in server-side operations
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Security Configuration
NODE_ENV=development

# Application Configuration
APP_NAME=Station-2100
APP_VERSION=1.0.0

# Rate Limiting Configuration (optional)
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_WINDOW_MS=900000

# Session Configuration
SESSION_SECRET=your-session-secret-here
SESSION_MAX_AGE=3600000

# Development flags
ENABLE_DEBUG_LOGS=false
ENABLE_MOCK_DATA=false

# Security Notes:
# 1. Never commit this file with real values
# 2. Service role key should only be used in server-side operations
# 3. Generate strong, unique secrets for production
# 4. Rotate keys regularly
# 5. Use environment-specific configurations